{
  "name": "backend",
  "version": "1.0.0",
  "description": "This backend server for chat app",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run start:dev",
    "client": "cd client && npm start",
    "start:dev": "concurrently \"nodemon --ignore 'client/*'\" \"npm run client\""
  },
  "author": "Subhash",
  "license": "MIT",
  "dependencies": {
    "express": "^4.17.1",
    "socket.io": "^4.1.2"
  },
  "devDependencies": {
    "concurrently": "^6.2.0"
  }
}
